import { LoadingPageView } from "@/components/loading-page-view";

interface LoadingProps {}

const Loading: React.FC<LoadingProps> = ({}) => {
    return (
        <>
            <LoadingPageView />
        </>
    );
};
export default Loading;
